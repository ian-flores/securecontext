% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed-external.R
\name{embed_openai}
\alias{embed_openai}
\title{OpenAI embeddings via the REST API}
\usage{
embed_openai(
  model = "text-embedding-3-small",
  dims = 1536L,
  api_key = Sys.getenv("OPENAI_API_KEY"),
  base_url = "https://api.openai.com/v1",
  batch_size = 64L,
  timeout = 30
)
}
\arguments{
\item{model}{Model identifier. Defaults to
\code{"text-embedding-3-small"} (1536 dims, \$0.02/1M tokens as of
late 2025).}

\item{dims}{Expected dimensionality. Defaults to \code{1536L} which
matches \code{text-embedding-3-small}. Pass the correct value for other
models (e.g. \code{3072L} for \code{text-embedding-3-large}).}

\item{api_key}{OpenAI API key. Defaults to \code{OPENAI_API_KEY} env var.}

\item{base_url}{API base URL (override for Azure OpenAI or proxies).}

\item{batch_size}{Number of texts per POST.}

\item{timeout}{Per-request timeout in seconds.}
}
\value{
A \link{securecontext_embedder} object.
}
\description{
Convenience wrapper that posts to \verb{POST /v1/embeddings} on the OpenAI
API. Requires \code{httr2} (Suggests). Batches requests at \code{batch_size}
per call. This does NOT wrap ellmer; ellmer does not currently expose
an embedding API (chat-only). Use \code{\link[=embed_custom]{embed_custom()}} if you prefer to
route through a different SDK.
}
\details{
Privacy note: unlike \code{\link[=embed_tfidf]{embed_tfidf()}}, this sends text to OpenAI. Only
enable it when that is acceptable for your data classification.
}
\examples{
\dontrun{
emb <- embed_openai()
embed_texts(emb, c("hello world"))
}
}
