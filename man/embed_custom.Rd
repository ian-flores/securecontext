% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed-external.R
\name{embed_custom}
\alias{embed_custom}
\title{Wrap an arbitrary embedding function as a securecontext embedder}
\usage{
embed_custom(fn, dims, name = "custom")
}
\arguments{
\item{fn}{A function \code{function(texts) -> matrix} where
\code{ncol(result) == dims} and \code{nrow(result) == length(texts)}.}

\item{dims}{Integer, the dimensionality of the embedding space.}

\item{name}{Character scalar, an informational label used in
diagnostic output. Defaults to \code{"custom"}.}
}
\value{
A \link{securecontext_embedder} object.
}
\description{
Generic plug-in point for external embedders. \code{fn} must be a function
that takes a character vector and returns a numeric matrix with one
row per input text and \code{dims} columns. The function is called inside
\code{\link[=embed_texts]{embed_texts()}} and therefore inherits its tracing span if a trace is
active.
}
\details{
Unlike \code{\link[=embed_tfidf]{embed_tfidf()}}, no local corpus is needed; you are responsible
for ensuring the underlying model produces the dimensionality you
declare.
}
\examples{
fake <- function(texts) matrix(runif(length(texts) * 8L), ncol = 8L)
emb <- embed_custom(fake, dims = 8L, name = "fake")
embed_texts(emb, c("hello", "world"))
}
