# Encrypt raw data with AES-256-CBC

Encrypt raw data with AES-256-CBC

## Usage

``` r
encrypt_raw(data, key)
```

## Arguments

- data:

  Raw vector to encrypt.

- key:

  Raw 32-byte key.

## Value

Encrypted raw vector (IV prepended).
